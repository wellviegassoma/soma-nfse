# Token compartilhado exigido em toda requisição (header X-Internal-Token).
# Mesmo valor precisa estar configurado no frontend (NFSE_ENGINE_INTERNAL_TOKEN
# e NFSE_ENGINE_URL apontando pra esse serviço).
NFSE_ENGINE_INTERNAL_TOKEN=
